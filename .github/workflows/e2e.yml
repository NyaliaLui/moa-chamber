name: Playwright E2E Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest

    env:
      NEXT_PUBLIC_WIX_CLIENT_ID: ${{ secrets.NEXT_PUBLIC_WIX_CLIENT_ID }}
      NEXT_PUBLIC_WIX_COLLECTION_TEAM: ${{ secrets.NEXT_PUBLIC_WIX_COLLECTION_TEAM }}
      NEXT_PUBLIC_WIX_COLLECTION_BOARD_MEMBERS: ${{ secrets.NEXT_PUBLIC_WIX_COLLECTION_BOARD_MEMBERS }}
      NEXT_PUBLIC_WIX_COLLECTION_MEMBERS: ${{ secrets.NEXT_PUBLIC_WIX_COLLECTION_MEMBERS }}
      NEXT_PUBLIC_WIX_COLLECTION_CULTURE_RESOURCES: ${{ secrets.NEXT_PUBLIC_WIX_COLLECTION_CULTURE_RESOURCES }}
      NEXT_PUBLIC_WIX_COLLECTION_BUSINESS_RESOURCES: ${{ secrets.NEXT_PUBLIC_WIX_COLLECTION_BUSINESS_RESOURCES }}
      NEXT_PUBLIC_WIX_COLLECTION_BENEFITS: ${{ secrets.NEXT_PUBLIC_WIX_COLLECTION_BENEFITS }}
      NEXT_PUBLIC_WIX_COLLECTION_TESTIMONIALS: ${{ secrets.NEXT_PUBLIC_WIX_COLLECTION_TESTIMONIALS }}
      NEXT_PUBLIC_WIX_COLLECTION_CTA: ${{ secrets.NEXT_PUBLIC_WIX_COLLECTION_CTA }}
      NEXT_PUBLIC_WIX_COLLECTION_CALENDAR: ${{ secrets.NEXT_PUBLIC_WIX_COLLECTION_CALENDAR }}
      NEXT_PUBLIC_WIX_COLLECTION_HOME: ${{ secrets.NEXT_PUBLIC_WIX_COLLECTION_HOME }}
      RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
      RESEND_TARGET_EMAIL: ${{ secrets.RESEND_TARGET_EMAIL }}

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: lts/*
      - name: Install dependencies
        run: npm ci
      - name: Install Playwright Browsers
        run: npx playwright install --with-deps
      - name: Make prouction build
        run: npm run build
      - name: Run Playwright tests
        run: npx playwright test
      - uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
